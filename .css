body {
    background: #f7ced7;
    margin: 0;
    overflow: hidden;
    font-family: 'Poppins', sans-serif;
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
    transition: background 2.5s ease-in-out;
}

body.dark-mode { background: #0a0a0c; }

/* THE MOON */
#moon {
    position: absolute;
    top: -150px; right: 10%;
    width: 100px; height: 100px;
    background: #fffdf2;
    border-radius: 50%;
    box-shadow: 0 0 60px rgba(255, 253, 242, 0.6);
    transition: transform 3.5s cubic-bezier(0.19, 1, 0.22, 1);
    z-index: 1;
}
body.dark-mode #moon { transform: translateY(240px); }

/* THE CARD STYLE */
.card {
    background: white;
    padding: 40px;
    border-radius: 40px;
    text-align: center;
    width: 350px;
    border: 8px solid rgba(255, 255, 255, 0.6);
    box-shadow: 0 10px 40px rgba(0, 0, 0, 0.1);
    z-index: 10;
    transition: all 1s ease;
}

body.dark-mode .card {
    background: rgba(255, 255, 255, 0.08);
    backdrop-filter: blur(15px);
    border-color: rgba(255, 255, 255, 0.15);
    color: white;
}

.envelope-icon { font-size: 50px; margin-bottom: 10px; animation: float 3s infinite ease-in-out; }
@keyframes float { 0%, 100% { transform: translateY(0); } 50% { transform: translateY(-10px); } }

.hidden { display: none; }

.title { font-family: 'Dancing Script', cursive; color: #e35d6a; font-size: 2.2rem; }
body.dark-mode .title { color: white; }

input {
    width: 85%; padding: 12px; border: 2px solid #f29ca9;
    border-radius: 25px; margin: 20px 0; text-align: center; outline: none;
}

.main-btn, .reset-btn {
    background: #e35d6a; color: white; border: none; padding: 12px 35px;
    border-radius: 30px; cursor: pointer; font-weight: 600;
}

/* TULIP & VISUALS */
.visual-container { display: flex; justify-content: center; align-items: flex-end; gap: 30px; margin: 30px 0; height: 200px; }

.tulip { position: relative; width: 60px; height: 100%; }
.stem { 
    width: 6px; height: 0; background: #4a8c44; 
    position: absolute; bottom: 0; left: 50%; transform: translateX(-50%); border-radius: 10px;
}

.leaf { position: absolute; background: #4a8c44; width: 35px; height: 18px; opacity: 0; transform: scale(0); }
.leaf-left { bottom: 40px; left: -25px; border-radius: 40px 0; transform-origin: right; }
.leaf-right { bottom: 65px; right: -25px; border-radius: 0 40px; transform-origin: left; }

.tulip-head { position: absolute; bottom: 150px; left: 50%; transform: translateX(-50%) scale(0); width: 50px; height: 70px; }
.p { position: absolute; width: 100%; height: 100%; background: #ff4d7d; border-radius: 50% 50% 40% 40%; }
.p-left { transform: rotate(-15deg); left: -12px; background: #ff6b95; }
.p-right { transform: rotate(15deg); left: 12px; background: #ff6b95; }
.p-back { transform: scale(0.9); background: #d42a5a; }

/* REVEAL ANIMATIONS */
body.dark-mode .stem { animation: grow-stem 2s forwards; }
body.dark-mode .leaf-left { animation: leaf-pop 0.8s 1.2s forwards; }
body.dark-mode .leaf-right { animation: leaf-pop 0.8s 1.4s forwards; }
body.dark-mode .tulip-head { animation: bloom 1.2s 2s cubic-bezier(0.17, 0.89, 0.32, 1.49) forwards; }

@keyframes grow-stem { to { height: 150px; } }
@keyframes leaf-pop { to { opacity: 1; transform: scale(1); } }
@keyframes bloom { to { transform: translateX(-50%) scale(1.2); } }

/* IMAGE & NOTE */
.image-section { display: flex; flex-direction: column; align-items: center; gap: 8px; }
.image-frame { width: 120px; height: 150px; border-radius: 20px; border: 4px solid white; opacity: 0; overflow: hidden; }
body.dark-mode .image-frame { animation: leaf-pop 1s 2.8s forwards; }
#random-pic { width: 100%; height: 100%; object-fit: cover; }

.note-tag { 
    font-family: 'Dancing Script', cursive; font-size: 1.1rem; color: white; 
    background: #e35d6a; padding: 2px 12px; border-radius: 12px; opacity: 0; 
}
body.dark-mode .note-tag { animation: leaf-pop 0.8s 3.2s forwards; }

#rizz-text { font-family: 'Dancing Script', cursive; font-size: 1.4rem; color: #e35d6a; margin: 15px 0; }
body.dark-mode #rizz-text { color: #ffb7c5; }

/* FOOTER AREA */
.thanks-note { font-family: 'Dancing Script', cursive; font-size: 1.6rem; color: #e35d6a; margin: 10px 0 5px 0; }
body.dark-mode .thanks-note { color: #ffb7c5; }

.ig-button {
    display: inline-flex; align-items: center; gap: 8px;
    background: linear-gradient(45deg, #f09433, #dc2743, #bc1888);
    color: white; text-decoration: none; padding: 8px 18px;
    border-radius: 50px; font-size: 0.85rem; margin-bottom: 20px;
}

.reveal-delay { opacity: 0; }
body.dark-mode .reveal-delay { animation: leaf-pop 1s 3.5s forwards; }
